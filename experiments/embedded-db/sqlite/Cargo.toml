[package]
name = "iris-sqlite-spike"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
sqlx = { version = "=0.9.0", default-features = false, features = ["runtime-tokio", "sqlite", "migrate", "macros"] }
sha2.workspace = true

[dev-dependencies]
tokio.workspace = true
tempfile.workspace = true
